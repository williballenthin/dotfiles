#!/usr/bin/env bash
watch_file .env/devshell.toml
use flake .env

# override virtualenv location from .direnv/python-3.xx/ to .venv
# which is expected by uv
export VIRTUAL_ENV=".venv"

layout python
export PYTHONBREAKPOINT="ipdb.set_trace"
