theme = "graphite"   # graphite, midnight, paper, ember
mode = "auto"        # auto, split, stack
vcs = "git"          # git, jj
exclude_untracked = true
line_numbers = true
wrap_lines = true
agent_notes = false
