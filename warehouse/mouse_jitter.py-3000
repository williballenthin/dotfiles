import time, logging

from pymouse import PyMouse

logger = logging.getLogger("mouse_jitter")
p = PyMouse()
flopper = 1
while True:
    (x, y) = p.position()
    (x, y) = (x + (1 * flopper), y)

    logger.info("mouse jittering: %d %d", x, y)
    p.move(x, y)

    flopper *= -1
    time.sleep(120)
